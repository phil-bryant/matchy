#pragma once
#include <string>

namespace matchycore
{ // Port of the matchy Python package version surface (pyproject.toml). Bumped manually per release.
 // #R001: Matchycore traceability requirement anchor for version surface.
 std::string Version();
}
